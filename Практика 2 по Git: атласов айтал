# Проверка параметров user.name и user.email в настройках системы Git
git config --global --list

# Переход в каталог для проектов, клонирование репозитория и переход в каталог репозитория
cd "каталог проектов"
git clone "URL-репозитория"
cd "каталог репозитория"

# Проверка имеющихся веток, создание новой ветки feature-nod-nok, переключение в нее и проверка имеющихся веток снова
git branch -a
git branch feature-nod-nok
git checkout feature-nod-nok
git branch -a

# Написание кода внутри функции nod(), определение наибольшего общего делителя чисел a и b, и выполнение первого коммита
git status
git add "измененные файлы"
git commit -m "сообщение"

# Написание кода внутри функции nok(), определение наименьшего общего кратного чисел a и b, и выполнение второго коммита
git status
git add "измененные файлы"
git commit -m "сообщение"

# Отправка изменений в GitHub
git push -u origin feature-nod-nok

# Переключение на ветку main
git checkout main

# Выполнение объединения с веткой feature-nod-nok
git merge feature-nod-nok

# Отправка изменений в GitHub
git push

# Проверка произведенных изменений в GitHub
# Найдите кнопку переключающие ветки. Найдите историю коммитов. Проверьте вкладку Actions.
